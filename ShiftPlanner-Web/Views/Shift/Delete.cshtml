@model Shift_Planner_Web.Models.Shift

@{
    ViewData["Title"] = "Delete Shift";
}

<div class="row justify-content-center">

    <div class="col-lg-6">

        <div class="card border-danger">

            <div class="card-body p-4">

                <div class="text-center mb-4">

                    <h1 class="text-danger fw-bold mb-2">
                        Delete Shift
                    </h1>

                    <p class="text-muted mb-0">
                        This action cannot be undone.
                    </p>

                </div>

                <div class="alert alert-danger">

                    <strong>
                        Warning:
                    </strong>

                    This shift will be permanently removed from the schedule.

                </div>

                <div class="card bg-light border-0 mb-4">

                    <div class="card-body">

                        <div class="row g-3">

                            <div class="col-md-6">

                                <div class="text-muted small">
                                    Employee
                                </div>

                                <div class="fw-semibold">
                                    @Model.Employee?.Name
                                </div>

                            </div>

                            <div class="col-md-6">

                                <div class="text-muted small">
                                    Date
                                </div>

                                <div>
                                    @Model.StartTime.ToString("dd MMM yyyy")
                                </div>

                            </div>

                            <div class="col-md-6">

                                <div class="text-muted small">
                                    Start Time
                                </div>

                                <div>
                                    @Model.StartTime.ToString("HH:mm")
                                </div>

                            </div>

                            <div class="col-md-6">

                                <div class="text-muted small">
                                    End Time
                                </div>

                                <div>
                                    @Model.EndTime.ToString("HH:mm")
                                </div>

                            </div>

                            <div class="col-md-12">

                                <div class="text-muted small">
                                    Shift Duration
                                </div>

                                <div>
                                    @Model.ShiftHours hrs
                                </div>

                            </div>

                        </div>

                    </div>

                </div>

                <form asp-action="Delete"
                      method="post">

                    <input asp-for="ShiftID"
                           type="hidden" />

                    <div class="d-flex justify-content-between">

                        <a asp-action="Index"
                           class="btn btn-outline-secondary">

                            Cancel

                        </a>

                        <button type="submit"
                                class="btn btn-danger">

                            Delete Shift

                        </button>

                    </div>

                </form>

            </div>

        </div>

    </div>

</div>